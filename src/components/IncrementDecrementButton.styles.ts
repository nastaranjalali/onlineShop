import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  root: {
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginTop: 20,
  },
  button: { backgroundColor: "transparent", border: "none" },
  buttonT: { fontSize: 30, color: "#07b377" },
});

export default useStyles;
